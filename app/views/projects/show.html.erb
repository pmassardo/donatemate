<h1><%= @project.title %></h1>
<p>
  Description: <%= @project.description %> <br>
  Funding Goal: <%= @project.goal %> <br>
  Start Date: <%= @project.start_date.strftime("%m/%d/%Y") %> <br>
  End Date: <%= @project.end_date.strftime("%m/%d/%Y") %> <br>
  Total Donated: $<%= @project.calc_total %> <br>
  Days Left: <%= (@project.end_date.to_date - @project.start_date.to_date).to_i %> <br>
</p>


<ul>
  <% @rewards.each do |reward| %>
  <li class = "rewards">Reward name: <%= reward.name %> <br>
      Description: <%= reward.description %> <br>
      Pledge Amount: $ <%= reward.level %> <br>
  <% end %>
  </li>
</ul>
<% if current_user %>
  <%= link_to "Donate Mate!", new_project_pledge_path(@project), class: "donate-btn" %>
<% end %>
